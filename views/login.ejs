<!DOCTYPE html>
<html lang="en">
    <%-include("layouts/header");-%>
<body>
    <section class="flex font-inter">
        <div class="bg-sky-400 w-1/2 h-screen login-page__background"></div>
        <div class="w-1/2 p-5 login-page__content">
            <div class="login-page__logo flex">
                <a href="/">
                    <img src="/assets/favico.png" alt="cilikly-logo">
                </a>
            </div>
            <h1 class="pl-6 text-3xl font-bold mt-4">Login</h1>
            <div class="mt-3 w-full text-center login-page__login-form">
                <h2 class="text-4xl mb-2">Welcome Back</h2>
                <p class="mb-3 px-28 text-slate-800">To start using cilikly please login below with your username and password</p>
				 
				<% if(typeof error != "undefined") { %>
					<% if(error.length > 0) { %>
						<% for(let i = 0; i<error.length; i++) {%>
							<div class="mx-36 px-4 py-2 text-left bg-red-300 rounded-sm"><%= error[i] %></div>
						<% } %>
					<% } %>
				<% } %>

				<form class="mt-5 px-36 text-left" action="/users/login" method="POST">
                    <label class="block mb-2" for="username">Username</label>
					<input class="mb-3 w-full block rounded-lg bg-slate-100 px-3 py-2" id="username" name="username" type="text" value="<%= username %>">

                    <label class="block mb-2" for="password">Password</label>
					<input class="w-full block rounded-lg bg-slate-100 px-3 py-2" id="password" name="password" type="password" value="<%= password %>">

                    <button class="mt-5 px-7 py-3 bg-sky-600 rounded-md hover:bg-sky-800 text-white" type="submit">Login</button>
                </form>
                <p class="mt-9 text-left px-36 text-slate-600">Don't have an account yet? <a class="font-bold text-sky-600 hover:text-slate-700" href="/auth/signup">Sign up</a></p>
            </div>
        </div>
    </section>
</body>
</html>
